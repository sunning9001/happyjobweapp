function splitStr(strArr){
  let str = strArr || ""
  let arr = strArr.split(",")
  if(arr.length==1 && arr[0]=="" ){
      return []
  }else{
    return arr 
  }
}

module.exports = {
  splitStr: splitStr
}